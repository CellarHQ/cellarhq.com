<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<configuration xmlns="http://www.jooq.org/xsd/jooq-codegen-3.4.0.xsd">
    <!-- Configure the database connection here -->
    <jdbc>
        <driver>org.postgresql.Driver</driver>
        <url>jdbc:postgresql://localhost:15432/cellarhq</url>
        <user>cellarhq</user>
        <password>cellarhq</password>
    </jdbc>

    <generator>
        <!-- The default code generator. You can override this one, to generate your own code style
             Defaults to org.jooq.util.DefaultGenerator -->
        <name>org.jooq.util.DefaultGenerator</name>

        <database>
            <!-- The database type. The format here is:
                 org.util.[database].[database]Database -->
            <name>org.jooq.util.postgres.PostgresDatabase</name>

            <!-- The database schema (or in the absence of schema support, in your RDBMS this
                 can be the owner, user, database name) to be generated -->
            <inputSchema>public</inputSchema>

            <!-- All elements that are generated from your schema
                 (A Java regular expression. Use the pipe to separate several expressions)
                 Watch out for case-sensitivity. Depending on your database, this might be important! -->
            <includes>.*</includes>

            <!-- All elements that are excluded from your schema
                 (A Java regular expression. Use the pipe to separate several expressions).
                 Excludes match before includes -->
            <excludes>databasechange.*</excludes>

            <!-- Optimistic locking -->
            <recordVersionFields>version</recordVersionFields>

            <!-- JSR 310 support? -->
            <customTypes>
                <customType>
                    <name>LocalDate</name>
                    <type>java.time.LocalDate</type>
                    <converter>com.cellarhq.jooq.LocalDateConverter</converter>
                </customType>
                <customType>
                    <name>LocalDateTime</name>
                    <type>java.time.LocalDateTime</type>
                    <converter>com.cellarhq.jooq.LocalDateTimeConverter</converter>
                </customType>
            </customTypes>
            <forcedTypes>
                <forcedType>
                    <name>LocalDate</name>
                    <types>date</types>
                    <expression>.*</expression>
                </forcedType>
                <forcedType>
                    <name>LocalDateTime</name>
                    <types>timestamp</types>
                    <expression>.*</expression>
                </forcedType>
            </forcedTypes>
        </database>

        <generate>
            <pojos>true</pojos>
            <jpaAnnotations>true</jpaAnnotations>
        </generate>

        <target>
            <!-- The destination package of your generated classes (within the destination directory) -->
            <packageName>com.cellarhq.generated</packageName>

            <!-- The destination directory of your generated classes -->
            <directory>src/main/java</directory>
        </target>
    </generator>
</configuration>
